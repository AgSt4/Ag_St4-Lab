

# Introducción

Contenido...
```

---

## Cómo funciona

1. **GitHub Actions ejecuta `quarto render`** → Renderiza TODO automáticamente
2. **Cualquier nota en cualquier subcarpeta** hereda la configuración de `_metadata.yml`
3. **Solo 2 archivos de configuración** en la raíz
4. **Cambiar de estilo** = cambiar 1 línea en el frontmatter (`brand: personal` o `brand: uc`)
5. **Zero configuración por carpeta** → Escalable infinitamente

---

## Estructura final
```
proyecto/
├── _quarto.yml           # ← Formatos globales
├── _metadata.yml         # ← Configuración global (1 solo archivo)
├── custom.scss
├── templates/
│   ├── doc_template.typ
│   ├── logo.png
│   ├── Logo UC.png
│   └── UC COLOR-01.png
└── content/
    └── notes/
        ├── library/
        │   └── macro/
        │       └── teoria-crecimiento.qmd
        ├── projects/
        │   └── otra-nota.qmd
        └── topics/
            └── otra-mas.qmd

::: {.callout-note title="Definición: Capital Humano"}
El capital humano se refiere al valor económico de las habilidades y experiencia de un trabajador.
:::
