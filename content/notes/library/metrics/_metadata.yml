# SOBRESCRIBIMOS RUTAS PARA NIVEL 4 (Metrics)
# Usamos ../../../ para llegar a la raíz correcta

# 1. Configuración general
metadata-files: 
  - "../../../templates/formats.yml" 

# 2. Rutas específicas que causaban el error "NotFound"
format:
  typst:
    template: "../../../templates/doc_template.typ"
    logo_file: "../../../templates/logo.png"
    
  beamer:
    header-includes:
      - \titlegraphic{\hfill\includegraphics[height=1.2cm]{../../../templates/logo.png}}